# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $m Mod4

set $a Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 8
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

#base03 darkest, background
#base02 background highlights
#base01 comments/secondary content
#base10 body text/ primary content
#base11 optional emphisised content
#base13 lightest

set $base03    #002b36
set $base02    #073642
set $base01    #586e75
set $base00    #657b83
set $base10    #839496
set $base11    #93a1a1
set $base12    #eee8d5
set $base13    #fdf6e3
set $yellow    #b58900
set $orange    #cb4b16
set $red       #dc322f
set $magenta   #d33682
set $violet    #6c71c4
set $blue      #268bd2
set $cyan      #2aa198
set $green     #859900
bar {
        status_command i3status
        wheel_up_cmd nop
        wheel_down_cmd nop
        #strip_workspace_numbers yes
        position top

        colors {
                background $base03
                separator  $base01
                statusline $base10
                # class             border   backgr.  text
                urgent_workspace    $magenta $base02  $magenta
                inactive_workspace  $base01  $base02  $base01
                active_workspace    $base11  $base02  $base11
                focused_workspace   $base11  $base03  $base11
        }
}
# class                 border   backgr.  text     indicator
client.focused          $base11  #00596f  $base13  $blue
client.focused_inactive $base10  $base03  $base10  $blue
client.unfocused        $base10  $base03  $base10  $blue
client.urgent           $base10  $magenta $base10  $blue
client.placeholder      $base10  $cyan    $base10  $blue

#disable urgent delay
force_display_urgency_hint 0 ms


# switch to workspace
bindsym $a+1 workspace 1
bindsym $a+2 workspace 2
bindsym $a+3 workspace 3
bindsym $a+4 workspace 4
bindsym $a+5 workspace 5
bindsym $a+6 workspace 6
bindsym $a+7 workspace 7
bindsym $a+8 workspace 8
bindsym $a+9 workspace 9
bindsym $a+0 workspace 10

# move focused container to workspace
bindsym $a+Shift+1 move container to workspace 1; workspace 1
bindsym $a+Shift+2 move container to workspace 2; workspace 2
bindsym $a+Shift+3 move container to workspace 3; workspace 3
bindsym $a+Shift+4 move container to workspace 4; workspace 4
bindsym $a+Shift+5 move container to workspace 5; workspace 5
bindsym $a+Shift+6 move container to workspace 6; workspace 6
bindsym $a+Shift+7 move container to workspace 7; workspace 7
bindsym $a+Shift+8 move container to workspace 8; workspace 8
bindsym $a+Shift+9 move container to workspace 9; workspace 9
bindsym $a+Shift+0 move container to workspace 10; workspace 10

# allow to goto last workspace
bindsym $a+Tab workspace back_and_forth
# allow last window back and forth
exec --no-startup-id "/home/y2k/bin/last_window.sh"
bindsym $m+Tab [con_mark=_last] focus

#
bindsym $a+l workspace next
bindsym $a+h workspace prev
bindsym $a+Shift+l move container to workspace next; workspace next
bindsym $a+Shift+h move container to workspace prev; workspace prev

# start dmenu (a program launcher)
bindsym $m+p exec --no-startup-id dmenu-recent-aliases

# start a terminal
bindsym $m+Return exec --no-startup-id xterm

# kill focused window
bindsym $m+x kill

# change focus
bindsym $m+h focus left
bindsym $m+j focus down
bindsym $m+k focus up
bindsym $m+l focus right

# move focused window
bindsym $m+Shift+h move left
bindsym $m+Shift+j move down
bindsym $m+Shift+k move up
bindsym $m+Shift+l move right

# split in horizontal/vertical  orientation
bindsym $m+Ctrl+h split h
bindsym $m+Ctrl+j split v
bindsym $m+Ctrl+semicolon layout toggle split

# change container layout (stacked, tabbed, toggle split)
bindsym $m+Ctrl+k layout stacking
bindsym $m+Ctrl+l layout tabbed

# toggle fullscreen mode for the focused container
bindsym $m+F11 fullscreen toggle
bindsym $m+Ctrl+F11 fullscreen toggle

# toggle tiling / floating
bindsym $m+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $m+space focus mode_toggle

# focus the parent/child  container
bindsym $m+Ctrl+o focus parent
bindsym $m+Ctrl+m focus child

# Use Mouse+$m to drag floating windows to their wanted position
floating_modifier $m

#lock with xscreensaver
bindsym $m+z exec --no-startup-id "xscreensaver-command -lock"

#take a screenshot
bindsym Print exec --no-startup-id "scrot_dmv"

#jump to urgent
bindsym $m+u [urgent=latest] focus

#volume controls
bindsym F9 exec --no-startup-id ponymix --max-volume=192 increase 6; exec killall -USR1 i3status
bindsym F8 exec --no-startup-id ponymix --max-volume=192 decrease 6; exec killall -USR1 i3status
bindsym F7 exec --no-startup-id ponymix toggle; exec killall -USR1 i3status
bindsym $m+F9 exec pavucontrol
bindsym $m+F8 exec pavucontrol
bindsym $m+F7 exec pavucontrol

# The middle button and a modifer over any part of the window kills the window
bindsym --whole-window $m+button2 kill


# borders for new windows
# set up placements for new windows
new_window pixel 0
for_window [class="XTerm"] border normal 0
for_window [instance="crx_nckgahadagoaajjgafhacjanaoiihapd"] border normal 0
for_window [instance="editwithvim"] border pixel 0, floating enable, move absolute position 650 px 375 px, resize grow width 160 px, resize grow height 90 px
for_window [class="xtermfloat"] border pixel 0, floating enable, move absolute position 650 px 375 px, resize grow width 160 px, resize grow height 90 px
for_window [class="feh"] border pixel 2, floating enable, move absolute position 150 px 105 px
for_window [class="Renode"] floating enable, move down 70 px, resize set width 280 px, resize set height 330 px

assign [instance="crx_nckgahadagoaajjgafhacjanaoiihapd"] 10

